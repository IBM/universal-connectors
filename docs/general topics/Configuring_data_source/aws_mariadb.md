# Create and configure a MariaDB database instance

To create a new MariaDB instance, follow the instructions described [here](https://aws.amazon.com/getting-started/hands-on/create-mariadb-db/). When setting the properties under Additional Configuration, in the Log exports section select Audit log as the log types to publish to Amazon CloudWatch logs.
