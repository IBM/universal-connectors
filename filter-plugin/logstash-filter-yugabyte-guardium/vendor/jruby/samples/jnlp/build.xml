<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." default="default-task" name="jnlp">
  <description>
    A testbed for getting JRuby to work with web start.
  </description>

  <!-- First try to load machine-specific properties. -->
  <property file="build.properties"/>

  <!-- And then load the defaults. It seems backwards to set defaults AFTER 
  setting local overrides, but that's how Ant works. -->
  <property file="default.build.properties"/>

  <condition property="jnlp-***REMOVED***mple-keystore.exists">
    <available file="jnlp-***REMOVED***mple-keystore"/>
  </condition>
    
  <target name="generate-keystore" unless="jnlp-***REMOVED***mple-keystore.exists">
    <genkey alias="jnlp-***REMOVED***mple-keystore" keystore="jnlp-***REMOVED***mple-keystore" storepass="jnlp-***REMOVED***mple-keystore" keypass="jnlp-***REMOVED***mple-keystore">
      <dname>
        <param name="CN" value="JRuby"/>
        <param name="OU" value="JRuby"/>
        <param name="O"  value="jruby.org"/>
        <param name="C"  value="US"/>
      </dname>
    </genkey>
  </target>  

  <target name="clean" description="delete signed-jars and jnlp-***REMOVED***mple-keystore">
    <delete file="jnlp-***REMOVED***mple-keystore"/>
    <delete dir="jars"/>
    <delete dir="signed-jars"/>
  </target>  

  <target name="copy-jars" description="copy selected jars to the jars directory">
    <copy file="../../lib/jruby-console.jar" todir="jars" />
  </target>

  <target name="sign-jars" depends="copy-jars, generate-keystore">
    <mkdir dir="signed-jars"/>
    <signjar destDir="signed-jars"
      keystore="${keystore}"
      storepass="${storepass}"
      keypass="${keypass}"
      alias="${alias}" 
      lazy="false"
      preservelastmodified="true">
      <path>
        <fileset dir="jars" includes="*.jar" />
      </path>
      <flattenmapper />
    </signjar>
  </target>
  
  <target name="default-task" depends="sign-jars">
    <echo>
      Testing the jnlp ***REMOVED***mples:
      
        javaws jirb.jnlp
        
        javaws signed-jirb.jnlp
    </echo>
  </target>
</project>
