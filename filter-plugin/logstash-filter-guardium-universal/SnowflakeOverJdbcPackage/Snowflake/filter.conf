# Snowflake audit logs via JDBC input — uses the universal filter plugin
filter {
  # ---- Universal filter (replaces guardium_snowflake_filter{}) ---------------
  guardium_universal_filter {
    datasource => "snowflake"
  }
}
